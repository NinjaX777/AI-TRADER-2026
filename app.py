import streamlit as st
import yfinance as yf
import pandas as pd
from datetime import datetime

# HIGH-CONTRAST AND USER CUSTOMIZATION FACTORS st.set_page_config(page_title="AI Trader Elite", layout="wide")

# LIGHT/DARK MODE COMPONENT with Accessibility Mode def get_style(accessibility_mode=False): return """<style> /* Base Styles */ .stApp { background: linear-gradient(145deg, #0b0d17 0%, #1c2341 50%, #111421 100%) !important; background-attachment: fixed !important; } /* Optional Accessibility Mode with Bright Contrast */ {}.format(".stApp{background: white; color: black!}}document.runBody('st.title"... minimal"`). , }] (